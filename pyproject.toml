[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dustdevol"
version = "1.0.0"
dependencies = [
    "requests",
    'importlib-metadata; python_version<"3.10"',
    'astropy',
    'numpy',
    'scipy',
]
